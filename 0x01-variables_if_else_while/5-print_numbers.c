#include <stdio.h>
/**
 * main-print the nubmers decemale from 0 to 10
 *
 * Return: always 0 when success
 */
int main(void)
{
	int i;

	i = 46;

	while (i <= 57)
	{
		putchar(i);
		i++;
	}
putchar('\n');
return (0);
}
